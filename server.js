// This file is no longer necessary.
// Local development should be started with `npm run dev`, which uses Vite's dev server.
// Production deployment is handled entirely by Vercel's infrastructure, which
// serves the static output from the `dist` directory and runs serverless functions from the `api` directory.
